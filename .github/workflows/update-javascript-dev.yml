# name: Update Development Tracking Plans

# on:
#   push:
#     branches-ignore:
#       - main
#     paths:
#       - 'tracking-rules/server/**.yml'
#       - 'tracking-rules/javascript/**.yml'

# jobs:
#   update-tracking-plans:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v3

#     - name: Set up Node.js
#       uses: actions/setup-node@v3
#       with:
#         node-version: '14'

#     - name: Install dependencies
#       run: npm install

#     - name: Run update script for server
#       env:
#         SEGMENT_API_KEY: ${{ secrets.SEGMENT_PUBLIC_API_TOKEN }}
#         SEGMENT_TRACKING_PLAN_ID: ${{ secrets.DEV_SEGMENT_TRACKING_PLAN_ID_SERVER }}
#         PLAN_DIR: tracking-rules/server
#       run: node scripts/update-tracking-plan.js

#     - name: Run save script for server
#       env:
#         SEGMENT_API_KEY: ${{ secrets.SEGMENT_PUBLIC_API_TOKEN }}
#         SEGMENT_TRACKING_PLAN_ID: ${{ secrets.DEV_SEGMENT_TRACKING_PLAN_ID_SERVER }}
#         PLAN_DIR: plans/server
#       run: node scripts/save-tracking-plan.js

#     - name: Run update script for javascript
#       env:
#         SEGMENT_API_KEY: ${{ secrets.SEGMENT_PUBLIC_API_TOKEN }}
#         SEGMENT_TRACKING_PLAN_ID: ${{ secrets.DEV_SEGMENT_TRACKING_PLAN_ID_JAVASCRIPT }}
#         PLAN_DIR: tracking-rules/javascript
#       run: node scripts/update-tracking-plan.js

#     - name: Run save script for javascript
#       env:
#         SEGMENT_API_KEY: ${{ secrets.SEGMENT_PUBLIC_API_TOKEN }}
#         SEGMENT_TRACKING_PLAN_ID: ${{ secrets.DEV_SEGMENT_TRACKING_PLAN_ID_JAVASCRIPT }}
#         PLAN_DIR: plans/javascript
#       run: node scripts/save-tracking-plan.js

#     - name: Commit and push changes
#       run: |
#         git config --global user.name "github-actions[bot]"
#         git config --global user.email "github-actions[bot]@users.noreply.github.com"
#         git add plans/server/current-rules.json plans/javascript/current-rules.json
#         git commit -m "Update tracking plan rules [skip ci]" || echo "No changes to commit"
#         git push origin ${{ github.ref }}



# name: Update Development Tracking Plans

# on:
#   push:
#     branches-ignore:
#       - main
#     paths:
#       - 'tracking-rules/server/**'
#       - 'tracking-rules/javascript/**'
#       # Add paths for additional tracking plans here

# jobs:
#   update-tracking-plans:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v3
#       with:
#         fetch-depth: 2  # Fetch the last two commits so HEAD~1 is available

#     - name: Set up Node.js
#       uses: actions/setup-node@v3
#       with:
#         node-version: '14'

#     - name: Install dependencies
#       run: npm install

#     # Update and commit for server
#     - name: Run update script for server
#       run: |
#         git diff --name-only HEAD~1 HEAD | grep -q "tracking-rules/server" && \
#         SEGMENT_API_KEY=${{ secrets.SEGMENT_PUBLIC_API_TOKEN }} \
#         SEGMENT_TRACKING_PLAN_ID=${{ secrets.DEV_SEGMENT_TRACKING_PLAN_ID_SERVER }} \
#         PLAN_DIR=tracking-rules/server \
#         node scripts/update-tracking-plan.js || echo "No changes detected for server"

#     - name: Run save script for server
#       run: |
#         git diff --name-only HEAD~1 HEAD | grep -q "tracking-rules/server" && \
#         SEGMENT_API_KEY=${{ secrets.SEGMENT_PUBLIC_API_TOKEN }} \
#         SEGMENT_TRACKING_PLAN_ID=${{ secrets.DEV_SEGMENT_TRACKING_PLAN_ID_SERVER }} \
#         PLAN_DIR=plans/server \
#         node scripts/save-tracking-plan.js || echo "No changes detected for server"

#     - name: Commit and push changes for server
#       run: |
#         git diff --name-only HEAD~1 HEAD | grep -q "tracking-rules/server" && \
#         git config --global user.name "github-actions[bot]" && \
#         git config --global user.email "github-actions[bot]@users.noreply.github.com" && \
#         git add plans/server/current-rules.json && \
#         git commit -m "Update server tracking plan rules [skip ci]" || echo "No changes to commit" && \
#         git push origin ${{ github.ref }}

#     # Update and commit for javascript
#     - name: Run update script for javascript
#       run: |
#         git diff --name-only HEAD~1 HEAD | grep -q "tracking-rules/javascript" && \
#         SEGMENT_API_KEY=${{ secrets.SEGMENT_PUBLIC_API_TOKEN }} \
#         SEGMENT_TRACKING_PLAN_ID=${{ secrets.DEV_SEGMENT_TRACKING_PLAN_ID_JAVASCRIPT }} \
#         PLAN_DIR=tracking-rules/javascript \
#         node scripts/update-tracking-plan.js || echo "No changes detected for javascript"

#     - name: Run save script for javascript
#       run: |
#         git diff --name-only HEAD~1 HEAD | grep -q "tracking-rules/javascript" && \
#         SEGMENT_API_KEY=${{ secrets.SEGMENT_PUBLIC_API_TOKEN }} \
#         SEGMENT_TRACKING_PLAN_ID=${{ secrets.DEV_SEGMENT_TRACKING_PLAN_ID_JAVASCRIPT }} \
#         PLAN_DIR=plans/javascript \
#         node scripts/save-tracking-plan.js || echo "No changes detected for javascript"

#     - name: Commit and push changes for javascript
#       run: |
#         git diff --name-only HEAD~1 HEAD | grep -q "tracking-rules/javascript" && \
#         git config --global user.name "github-actions[bot]" && \
#         git config --global user.email "github-actions[bot]@users.noreply.github.com" && \
#         git add plans/javascript/current-rules.json && \
#         git commit -m "Update javascript tracking plan rules [skip ci]" || echo "No changes to commit" && \
#         git push origin ${{ github.ref }}


name: Update Development Tracking Plans

on:
  push:
    branches-ignore:
      - main
    paths:
      - 'tracking-rules/server/**'
      - 'tracking-rules/javascript/**'
      # Add paths for additional tracking plans here

jobs:
  update-tracking-plans:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 2  # Fetch the last two commits so HEAD^ is available

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '14'

    - name: Install dependencies
      run: npm install

    # Update, commit, and push for server
    - name: Run update script for server
      run: |
        echo "Checking for changes in server tracking plan"
        git diff --name-only HEAD^ HEAD
        git diff --name-only HEAD^ HEAD | grep -q "tracking-rules/server" && \
        SEGMENT_API_KEY=${{ secrets.SEGMENT_PUBLIC_API_TOKEN }} \
        SEGMENT_TRACKING_PLAN_ID=${{ secrets.DEV_SEGMENT_TRACKING_PLAN_ID_SERVER }} \
        PLAN_DIR=tracking-rules/server \
        node scripts/update-tracking-plan.js || echo "No changes detected for server"

    - name: Run save script for server
      run: |
        git diff --name-only HEAD^ HEAD | grep -q "tracking-rules/server" && \
        SEGMENT_API_KEY=${{ secrets.SEGMENT_PUBLIC_API_TOKEN }} \
        SEGMENT_TRACKING_PLAN_ID=${{ secrets.DEV_SEGMENT_TRACKING_PLAN_ID_SERVER }} \
        PLAN_DIR=plans/server \
        node scripts/save-tracking-plan.js || echo "No changes detected for server"

    - name: Commit and push changes for server
      run: |
        git diff --name-only HEAD^ HEAD | grep -q "tracking-rules/server" && \
        git config --global user.name "github-actions[bot]" && \
        git config --global user.email "github-actions[bot]@users.noreply.github.com" && \
        git add plans/server/current-rules.json && \
        git commit -m "Update server tracking plan rules [skip ci]" || echo "No changes to commit" && \
        git push origin ${{ github.ref }}

    # Fresh checkout for javascript
    - name: Checkout repository again for javascript
      uses: actions/checkout@v3
      with:
        fetch-depth: 2  # Ensure the last two commits are available

    # Update, commit, and push for javascript
    - name: Run update script for javascript
      run: |
        echo "Checking for changes in javascript tracking plan"
        git diff --name-only HEAD^ HEAD
        git diff --name-only HEAD^ HEAD | grep -q "tracking-rules/javascript" && \
        SEGMENT_API_KEY=${{ secrets.SEGMENT_PUBLIC_API_TOKEN }} \
        SEGMENT_TRACKING_PLAN_ID=${{ secrets.DEV_SEGMENT_TRACKING_PLAN_ID_JAVASCRIPT }} \
        PLAN_DIR=tracking-rules/javascript \
        node scripts/update-tracking-plan.js || echo "No changes detected for javascript"

    - name: Run save script for javascript
      run: |
        git diff --name-only HEAD^ HEAD | grep -q "tracking-rules/javascript" && \
        SEGMENT_API_KEY=${{ secrets.SEGMENT_PUBLIC_API_TOKEN }} \
        SEGMENT_TRACKING_PLAN_ID=${{ secrets.DEV_SEGMENT_TRACKING_PLAN_ID_JAVASCRIPT }} \
        PLAN_DIR=plans/javascript \
        node scripts/save-tracking-plan.js || echo "No changes detected for javascript"

    - name: Commit and push changes for javascript
      run: |
        git diff --name-only HEAD^ HEAD | grep -q "tracking-rules/javascript" && \
        git config --global user.name "github-actions[bot]" && \
        git config --global user.email "github-actions[bot]@users.noreply.github.com" && \
        git add plans/javascript/current-rules.json && \
        git commit -m "Update javascript tracking plan rules [skip ci]" || echo "No changes to commit" && \
        git push origin ${{ github.ref }}